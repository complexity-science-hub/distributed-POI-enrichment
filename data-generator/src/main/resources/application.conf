{
  application-name = "data ingest"
  osm-load = {
    node-path = "node.parquet"
    way-path = "way.parquet"
    relation-path = "relation.parquet"
    shared-prefix = "sample-data" # TODO set from makefile
    save-mode = "Overwrite"
    # output-folder = "phd/poi" # TODO set from makefile
  }
}